name: env

jobs:
  job1:
    runs-on: ubuntu-latest
    env:
      PROJECT_NAME: "zeroOps"
      AUTHOR: "DevOpsTeam"
    steps:
      - uses: actions/checkout@v4
      - run: echo "$PROJECT_NAME by $AUTHOR"